name: Build Main App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: org/.github/.github/workflows/dotnet-build.yml@main
    with:
      base-version: '1.3'
      runner: 'windows-net'
